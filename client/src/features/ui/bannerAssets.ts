const BASE = '/banner-assets';

export const RANKING_URL = '/ranking.svg';

export const BANNER_ASSETS = {
  fire: `${BASE}/vector-16.svg`,
  ranking: RANKING_URL,
  avatar: `${BASE}/avatar.png`,
} as const;
